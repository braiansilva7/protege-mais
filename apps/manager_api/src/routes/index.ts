import type { FastifyInstance } from 'fastify';

export const apiV1Prefix = '/api/v1';

export default function routes(_server: FastifyInstance) {
  // As rotas de domínio serão adicionadas pelos tickets funcionais.
}
